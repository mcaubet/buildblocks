#!/usr/bin/env modbuild

pbuild::configure() {
	"${MODULE_SRCDIR}"/configure \
		--prefix="${PREFIX}" \
		--with-pic=yes \
		--disable-shared \
		--disable-jpeg \
		|| exit 1
}

CC=gcc

pbuild::add_to_group 'Libraries'
pbuild::make_all
