#!/usr/bin/env modbuild

pbuild::configure() {
	"${MODULE_SRCDIR}"/configure \
		--prefix="${PREFIX}" \
		--enable-shared \
		--enable-cxx \
		--enable-unsupported \
		--enable-threadsafe \
		--with-pic \
		|| exit 1
}

pbuild::add_to_group 'Compiler'
pbuild::make_all

